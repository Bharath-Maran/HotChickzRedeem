# ── Database ──────────────────────────────────────────────────────────────────
# Supabase: Project → Settings → Database → Connection string (URI)
# For local PostgreSQL: postgresql://user:password@localhost:5432/hotchickz
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Set to "false" only for a local PostgreSQL instance without SSL
USE_SSL=true

# ── CORS ──────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=https://hotchickzoffer.com,http://localhost:5173

# ── GoHighLevel API ────────────────────────────────────────────────────────────
# Used to verify that an incoming contact_id is a real GHL contact before
# allowing a claim. Without this, all unknown contact IDs are rejected.
# Obtain from: GHL → Settings → API Keys (scoped to your location/sub-account)
GHL_API_KEY=your_ghl_api_key_here
